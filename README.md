# Test Project

This repository contains a simple demo of JavaScript for CSCI E-3 homework.

## Running Tests

1. Install dependencies (only needed once):
   ```bash
   npm install
   ```
2. Run the tests:
   ```bash
   npm test
   ```

The test simulates entering three numbers on `homework1.html` and verifies the total displayed in `resultArea`.
